import { StyleSheet } from 'react-native';

export const s = StyleSheet.create({
    input: {
        fontSize: 24,
        padding: 10,
        margin: 10,
        borderRadius: 5,
        backgroundColor: 'white',
        width: '100%'
    }
});